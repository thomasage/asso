{% extends ':param:layout.html.twig' %}

{% trans_default_domain 'param' %}

{% block body %}

    <div class="app_param_level_delete">

        {% include('param/_menu.html.twig') with {active:'level'} only %}

        {{ include('default/_flash_messages.html.twig') }}

        {{ form_start(formDelete) }}

        <div class="edit1">
            {{ form_errors(formDelete) }}
            <div class="notification">
                {{ 'delete_level.confirmation'|trans({'%level%':level}) }}
            </div>
            <div class="action">
                <button type="submit" class="btn btn-danger" name="delete">
                    <span class="fi-trash"></span>
                    {{ 'action.delete'|trans({},'messages') }}
                </button>
                <span>or</span>
                <a href="{{ path('app_param_level_edit',{level:level.id}) }}" class="btn btn-outline-secondary">
                    <span class="fi-x"></span>
                    {{ 'action.cancel'|trans({},'messages') }}
                </a>
            </div>
        </div>

        {{ form_end(formDelete) }}

    </div>

{% endblock %}
